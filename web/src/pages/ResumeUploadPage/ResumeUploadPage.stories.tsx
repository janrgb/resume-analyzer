import type { Meta, StoryObj } from '@storybook/react'

import ResumeUploadPage from './ResumeUploadPage'

const meta: Meta<typeof ResumeUploadPage> = {
  component: ResumeUploadPage,
}

export default meta

type Story = StoryObj<typeof ResumeUploadPage>

export const Primary: Story = {}
